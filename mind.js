const num1 = 5;
const num2 = 10;

function addValue () {
    console.log(`the sum is : ${num1 + num2}`);
}

function subValue () {
    console.log(`the sum is : ${num2 - num1}`);
}

addValue()
subValue()